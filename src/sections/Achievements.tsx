import codeFiesta from "../assets/images/codeFiesta.jpeg";
import Freshathon from "../assets/images/Freshathon.jpeg";
import dhruva from "../assets/images/dhruva.jpeg";
import mentor from "../assets/images/mentor.png";
import youthSummit from "../assets/images/youthSummit.jpeg";
import dataDrive from "../assets/images/dataDrive.jpeg";
import mentorBadge from "../assets/images/mentorBadge.jpeg";
import eloquence from "../assets/images/eloquence.jpg";
import coe1 from "../assets/images/coe1.jpeg";
import ineuData from "../assets/images/ineuData.jpeg";
import coe2 from "../assets/images/coe2.jpeg";
import IV from "../assets/images/IV.jpeg";
import mentorsDay from "../assets/images/mentorsDay.jpeg";
import juryPanel from "../assets/images/juryPanel.png";
import designathon from "../assets/images/designathon.jpg";
import MKCE from "../assets/images/MKCE.jpeg";
import agentathon from "../assets/images/agentathon.jpeg";
import caterpillar from "../assets/images/caterpillar.jpeg";

const achievementsList = [
  {
    title: "Code Fiesta",
    desc: "Secured 4th place competing among 60 participants by implementing a billing system using C programming",
    date: "Jan 2024",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: codeFiesta,
  },
  {
    title: "Freshathon",
    desc: "Awarded 1st for our project 'Green Horizon' which is an ML based plant growth analyzer ",
    date: "Jun 2024",
    rotation: "rotate-[3deg]",
    tapeClass: "paper-tape-right",
    image: Freshathon,
  },
  {
    title: "Dhruva 2024",
    desc: "Participated in various technical events",
    date: "Mar 2024",
    rotation: "rotate-[3deg]",
    tapeClass: "paper-tape-right",
    image: dhruva,
  },
  {
    title: "Student Mentor",
    desc: "After 4 rounds of selection process, got selected as Student Mentor",
    date: "Jan 2025",
    rotation: "rotate-[3deg]",
    tapeClass: "paper-tape-right",
    image: mentor,
  },
  {
    title: "Youth Summit",
    desc: "Participated in debate where various popular speakers spoke on the topic 'Gen Z vs Millenials'. I had opprtunity to share my thoughta and get insights from people like them.",
    date: "Feb 2025",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: youthSummit,
  },
  {
    title: "Datathon",
    desc: "Coordinated the event Datathon, where students from various colleges participated and solved problems. This made me strong in time management and coordination",
    date: "Feb 2025",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: dataDrive,
  },
  {
    title: "Student Mentor",
    desc: "Happy to receive the Mentor badge from our placement head.",
    date: "Mar 2025",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: mentorBadge,
  },
  {
    title: "Dhruva 2025 - Eloquence Challenge",
    desc: "Won 1st price in elloquence challenge where I was on-spot topic to speak before an auditorium of audience. This gave me confidence in public speaking and articulately put forward my thoughts",
    date: "Mar 2025",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: eloquence,
  },
  {
    title: "Data Science COE",
    desc: "Took seminar for my juniors on the topic 'Data and beyond' where various topics like data collection, preprocessing, ETL and more were discussed.",
    date: "Mar 2025",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: coe1,
  },
  {
    title: "Data Engineering - INEU Data",
    desc: "Built scalable real-time data pipelines leveraging Apache Kafka, Airflow, PySpark, and MongoDB to process and analyze highvolume datasets efficiently. Developed ETL, data validation, and transformation workflows, enabling accurate reporting and data-driven decision-making across analytical use cases. ",
    date: "Jul 2025",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: ineuData,
  },
  {
    title: "Data Science COE",
    desc: "Took seminar and conducted a hands-on session on PowerBI, Tableau and advanced plots using plotly",
    date: "Jul 2025",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: coe2,
  },
  {
    title: "Cognizant Technology Solutions - Industrial Visit",
    desc: "Visited CTS campus at Kochi. Attended sessions on Agile where we learnt how agile development works through an engaging activity",
    date: "Aug 2025",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: IV,
  },
  {
    title: "Mentors Day",
    desc: "Took part in a discussion on the topic 'My mentor, My inspiration'where I was one of the panel member.",
    date: "Oct 2025",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: mentorsDay,
  },
  {
    title: "Student mentor - Lead",
    desc: "After successfully being a student mentor for a year. I was given a position of Student mentor lead where I was responsible for the overall coordination and smooth running of mentoring sessions.",
    date: "Oct 2025",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: juryPanel,
  },
  {
    title: "Designathon",
    desc: "Participated in Designathon which was a design challenge. Collaborated with my mechanical team mates and designed an innovative solution for EV- bikes.",
    date: "Nov 2025",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: designathon,
  },
  {
    title: "Multiverse - Hackathon",
    desc: "Secured 3rd price in Multiverse - A national level hackathon conducted by MKCE Karur. It was a great experience coding for 36 hours and competing againts 100 teams which included both hardware and software projects.",
    date: "Feb 2025",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: MKCE,
  },
  {
    title: "AI Agentathon",
    desc: "Coordinated AI Agentathon, technical event hosted by Sri Eshwar College of Engineering for Thiran 2026. Had a opportunity to interact with different minds from different places. We also had international participants from Russia, Taiwan and more.",
    date: "Mar 2025",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: agentathon,
  },
  {
    title: "Caterpillar Tech Challenge",
    desc: "Participated in Caterpillar Tech challenge 2026 where me and my team cleared 3 rounds of selection. Though we couldn't make it to the finals, it was a great learning experience where we dived into concepts that are not our domain and got opportunity to connect with more people.",
    date: "Mar 2025",
    rotation: "rotate-[-2deg]",
    tapeClass: "paper-tape-left",
    image: caterpillar,
  },
];

import { useState } from "react";
import { createPortal } from "react-dom";

export default function Achievements() {
  const [currentPage, setCurrentPage] = useState(0); // 0 to 19 (which maps to Spreads 0 to 9 on desktop)
  const [isFlipping, setIsFlipping] = useState(false);
  const [flipDirection, setFlipDirection] = useState<"next" | "prev">("next");
  const [zoomImage, setZoomImage] = useState<{ src: string; title: string; date: string; desc: string } | null>(null);

  const totalPages = 20; // 0..19
  const totalSpreads = 10; // 0..9

  const flipNext = () => {
    if (isFlipping) return;
    const isMobile = typeof window !== "undefined" ? window.innerWidth < 768 : false;

    if (isMobile) {
      if (currentPage < totalPages - 1) {
        setFlipDirection("next");
        setIsFlipping(true);
        setTimeout(() => {
          setCurrentPage((prev) => prev + 1);
          setIsFlipping(false);
        }, 600);
      }
    } else {
      const activeSpread = Math.floor(currentPage / 2);
      if (activeSpread < totalSpreads - 1) {
        setFlipDirection("next");
        setIsFlipping(true);
        setTimeout(() => {
          setCurrentPage((prev) => {
            const nextSpread = Math.floor(prev / 2) + 1;
            return nextSpread * 2;
          });
          setIsFlipping(false);
        }, 600);
      }
    }
  };

  const flipPrev = () => {
    if (isFlipping) return;
    const isMobile = typeof window !== "undefined" ? window.innerWidth < 768 : false;

    if (isMobile) {
      if (currentPage > 0) {
        setFlipDirection("prev");
        setIsFlipping(true);
        setTimeout(() => {
          setCurrentPage((prev) => prev - 1);
          setIsFlipping(false);
        }, 600);
      }
    } else {
      const activeSpread = Math.floor(currentPage / 2);
      if (activeSpread > 0) {
        setFlipDirection("prev");
        setIsFlipping(true);
        setTimeout(() => {
          setCurrentPage((prev) => {
            const prevSpread = Math.floor(prev / 2) - 1;
            return prevSpread * 2;
          });
          setIsFlipping(false);
        }, 600);
      }
    }
  };

  const activeSpread = Math.floor(currentPage / 2);
  const leftPageIdx = activeSpread * 2;
  const rightPageIdx = activeSpread * 2 + 1;

  const renderPageContent = (p: number) => {
    if (p === 0) {
      return (
        <div className="h-full flex flex-col justify-center px-6 md:px-10 text-left select-none relative py-6">
          <span className="text-xs uppercase tracking-widest text-[#9B938B] font-bold font-sans block mb-2">
            Scrapbook Vol. I
          </span>
          <h2 className="handwritten-title text-3xl md:text-4xl leading-tight mb-4">
            Participations & Achievements
          </h2>
          <div className="w-16 h-1 bg-[#6F5E53] rounded mb-6"></div>
          <p className="handwritten-text text-sm md:text-base text-[#4F4A45] leading-relaxed mb-4">
            A small collection of notes, moments, and milestones from my journey.
          </p>
          <p className="handwritten-text text-sm md:text-base text-[#4F4A45] leading-relaxed">
            Here, I capture the moments of my student life where I stepped out of my comfort zone—coordinating events, public speaking, collaborating on hackathons, and exploring data engineering.
          </p>
          
          <div className="absolute bottom-6 right-6 flex items-center gap-2 text-[#9B938B] font-mono text-[10px] md:text-xs animate-pulse">
            <span>Flip Page</span>
            <svg className="w-4 h-4" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" d="M17.25 8.25L21 12m0 0l-3.75 3.75M21 12H3" />
            </svg>
          </div>
        </div>
      );
    }

    if (p === 19) {
      return (
        <div className="h-full flex flex-col justify-center px-6 md:px-10 text-left select-none relative py-6">
          <h2 className="handwritten-title text-3xl mb-4">
            The End of this Chapter
          </h2>
          <div className="w-16 h-1 bg-[#6F5E53] rounded mb-6"></div>
          <p className="handwritten-text text-sm md:text-base text-[#4F4A45] leading-relaxed mb-6">
            Thank you for flipping through my notebook! Every entry represents a challenge embraced and a lesson learned.
          </p>
          <p className="handwritten-text text-sm md:text-base text-[#4F4A45] leading-relaxed italic">
            "The next blank pages are waiting for new projects, learning paths, and achievements to be written..."
          </p>

          <div className="mt-8 flex justify-end">
            <div className="text-right">
              <span className="font-mono text-[10px] text-[#9B938B] block">Signed,</span>
              <span className="handwritten-title text-xl text-[#6F5E53] block mt-1">Vibisha</span>
            </div>
          </div>
        </div>
      );
    }

    const itemIndex = p - 1;
    const item = achievementsList[itemIndex];
    if (!item) return null;

    return (
      <div className="h-full flex flex-col justify-between py-6 px-4 select-none relative">
        {/* Polaroid Card */}
        <div
          className={`relative bg-white p-3 pb-5 rounded shadow-md border border-[#E6DFD5] w-full max-w-[245px] md:max-w-[270px] mx-auto transition-all duration-300 hover:scale-102 hover:rotate-0 hover:shadow-lg mt-2 cursor-zoom-in ${item.rotation}`}
          onClick={() => setZoomImage({ src: item.image, title: item.title, date: item.date, desc: item.desc })}
        >
          {/* Paper Tape */}
          <div className={item.tapeClass}></div>

          {/* Polaroid Picture */}
          <div className="w-full h-36 md:h-44 bg-gray-100 overflow-hidden border border-gray-200 rounded-sm">
            <img
              src={item.image}
              alt={item.title}
              className="w-full h-full object-cover"
            />
          </div>

          {/* Polaroid Caption */}
          <div className="mt-3 text-left">
            <h3 className="handwritten-text font-bold text-sm md:text-base leading-tight text-[#1E1B18] line-clamp-1">
              {item.title}
            </h3>
            <span className="handwritten-text text-[9px] md:text-[10px] text-[#9B938B] block font-mono mt-0.5">
              {item.date}
            </span>
          </div>
        </div>

        {/* Description below Polaroid */}
        <div className="mt-4 px-2 text-left flex-grow">
          <p className="handwritten-text text-[11px] md:text-xs text-[#4F4A45] leading-relaxed line-clamp-7 md:line-clamp-none">
            {item.desc}
          </p>
        </div>

        {/* Page Number Indicator */}
        <div className="text-center font-mono text-[9px] text-[#9B938B] pt-2">
          Page {p} of 18
        </div>
      </div>
    );
  };

  return (
    <div className="w-full h-full flex flex-col justify-center max-w-4xl mx-auto px-4 md:px-0 py-4 select-none">
      <div className="relative w-full h-[680px] bg-[#E6DFD5]/20 rounded-3xl p-3 md:p-6 shadow-2xl border border-[#E6DFD5]/50 backdrop-blur-xs">
        
        {/* Desktop Notebook (md and up) */}
        <div className="hidden md:flex relative w-full h-[580px] bg-[#FAF8F5] rounded-xl shadow-lg border border-[#E6DFD5] overflow-visible perspective-1500">
          
          {/* Central Spiral Spine */}
          <div className="notebook-spiral-double">
            <div className="spiral-ring-double"></div>
            <div className="spiral-ring-double"></div>
            <div className="spiral-ring-double"></div>
            <div className="spiral-ring-double"></div>
            <div className="spiral-ring-double"></div>
            <div className="spiral-ring-double"></div>
            <div className="spiral-ring-double"></div>
            <div className="spiral-ring-double"></div>
            <div className="spiral-ring-double"></div>
            <div className="spiral-ring-double"></div>
            <div className="spiral-ring-double"></div>
            <div className="spiral-ring-double"></div>
          </div>

          {!isFlipping ? (
            <>
              {/* Left Page */}
              <div className="w-1/2 h-full relative overflow-hidden notebook-page-left border-r border-[#E6DFD5]/40 rounded-l-xl">
                {renderPageContent(leftPageIdx)}
                {activeSpread > 0 && (
                  <div 
                    className="page-corner-prev"
                    onClick={flipPrev}
                    title="Previous Page"
                  ></div>
                )}
              </div>

              {/* Right Page */}
              <div className="w-1/2 h-full relative overflow-hidden notebook-page-right rounded-r-xl">
                {renderPageContent(rightPageIdx)}
                {activeSpread < totalSpreads - 1 && (
                  <div 
                    className="page-corner-next"
                    onClick={flipNext}
                    title="Next Page"
                  ></div>
                )}
              </div>
            </>
          ) : flipDirection === "next" ? (
            <>
              {/* Static Left Page (showing current left) */}
              <div className="w-1/2 h-full relative overflow-hidden notebook-page-left border-r border-[#E6DFD5]/40 rounded-l-xl z-10">
                {renderPageContent(leftPageIdx)}
              </div>

              {/* Static Right Page (showing NEXT right) */}
              <div className="w-1/2 h-full relative overflow-hidden notebook-page-right rounded-r-xl z-10">
                {renderPageContent((activeSpread + 1) * 2 + 1)}
              </div>

              {/* Flipping Page Container (Right half of the book, origin is the left edge at spine) */}
              <div 
                className="absolute top-0 right-0 w-1/2 h-full transform-style-3d animate-flip-forward z-20 rounded-r-xl"
                style={{ transformOrigin: "left center" }}
              >
                {/* Front Side (facing up initially, shows current right page) */}
                <div className="absolute inset-0 backface-hidden notebook-page-right rounded-r-xl">
                  {renderPageContent(rightPageIdx)}
                </div>

                {/* Back Side (facing down initially, shows NEXT left page, rotated 180deg around Y) */}
                <div 
                  className="absolute inset-0 backface-hidden notebook-page-left rounded-l-xl rotate-y-180 border-l border-[#E6DFD5]/40"
                  style={{ transform: "rotateY(180deg)" }}
                >
                  {renderPageContent((activeSpread + 1) * 2)}
                </div>
              </div>
            </>
          ) : (
            <>
              {/* Static Left Page (showing PREVIOUS left) */}
              <div className="w-1/2 h-full relative overflow-hidden notebook-page-left border-r border-[#E6DFD5]/40 rounded-l-xl z-10">
                {renderPageContent((activeSpread - 1) * 2)}
              </div>

              {/* Static Right Page (showing current right) */}
              <div className="w-1/2 h-full relative overflow-hidden notebook-page-right rounded-r-xl z-10">
                {renderPageContent(rightPageIdx)}
              </div>

              {/* Flipping Page Container (Left half of the book, origin is the right edge at spine) */}
              <div 
                className="absolute top-0 left-0 w-1/2 h-full transform-style-3d animate-flip-backward z-20 rounded-l-xl"
                style={{ transformOrigin: "right center" }}
              >
                {/* Front Side (facing up initially, shows current left page) */}
                <div className="absolute inset-0 backface-hidden notebook-page-left border-r border-[#E6DFD5]/40 rounded-l-xl">
                  {renderPageContent(leftPageIdx)}
                </div>

                {/* Back Side (facing down initially, shows PREVIOUS right page, rotated 180deg around Y) */}
                <div 
                  className="absolute inset-0 backface-hidden notebook-page-right rounded-r-xl rotate-y-180"
                  style={{ transform: "rotateY(180deg)" }}
                >
                  {renderPageContent((activeSpread - 1) * 2 + 1)}
                </div>
              </div>
            </>
          )}
        </div>

        {/* Mobile Notebook (below md, single page format) */}
        <div className="flex md:hidden relative w-full h-[580px] bg-[#FAF8F5] rounded-xl shadow-lg border border-[#E6DFD5] overflow-visible perspective-1500">
          {/* Vertical Spiral Rings on the Left Edge */}
          <div className="notebook-spiral">
            <div className="spiral-ring"></div>
            <div className="spiral-ring"></div>
            <div className="spiral-ring"></div>
            <div className="spiral-ring"></div>
            <div className="spiral-ring"></div>
            <div className="spiral-ring"></div>
            <div className="spiral-ring"></div>
            <div className="spiral-ring"></div>
            <div className="spiral-ring"></div>
            <div className="spiral-ring"></div>
            <div className="spiral-ring"></div>
            <div className="spiral-ring"></div>
          </div>

          <div className="w-full h-full relative overflow-hidden notebook-page-left rounded-xl">
            {renderPageContent(currentPage)}
            
            {/* Prev Corner */}
            {currentPage > 0 && (
              <div 
                className="page-corner-prev"
                onClick={flipPrev}
                title="Previous Page"
              ></div>
            )}

            {/* Next Corner */}
            {currentPage < totalPages - 1 && (
              <div 
                className="page-corner-next"
                onClick={flipNext}
                title="Next Page"
              ></div>
            )}
          </div>
        </div>

        {/* Scrapbook Bottom Navigation Panel */}
        <div className="flex justify-between items-center mt-4 px-2 select-none">
          <button
            onClick={flipPrev}
            disabled={currentPage === 0}
            className="px-4 py-2 text-xs md:text-sm font-bold font-sans bg-white border border-[#E6DFD5] rounded-xl hover:border-[#6F5E53] hover:shadow-xs disabled:opacity-30 disabled:pointer-events-none transition-all duration-300 cursor-pointer"
          >
            &larr; Prev Page
          </button>
          
          <span className="font-mono text-xs text-[#9B938B]">
            {currentPage < 20 && Math.floor(currentPage / 2) === 0 ? "Spread 1 / 10" : `Spread ${activeSpread + 1} / ${totalSpreads}`}
          </span>

          <button
            onClick={flipNext}
            disabled={currentPage >= totalPages - 1}
            className="px-4 py-2 text-xs md:text-sm font-bold font-sans bg-white border border-[#E6DFD5] rounded-xl hover:border-[#6F5E53] hover:shadow-xs disabled:opacity-30 disabled:pointer-events-none transition-all duration-300 cursor-pointer"
          >
            Next Page &rarr;
          </button>
        </div>

      </div>

      {zoomImage && createPortal(
        <div
          className="fixed inset-0 z-[100] flex items-center justify-center bg-black/75 backdrop-blur-xs p-4 cursor-zoom-out animate-fade-in"
          onClick={() => setZoomImage(null)}
        >
          {/* Modal Container */}
          <div
            className="bg-[#FAF6F0] border border-[#E6DFD5] w-full max-w-2xl rounded-2xl shadow-2xl p-6 relative flex flex-col items-center gap-4 cursor-default max-h-[90vh] overflow-hidden animate-scale-up"
            onClick={(e) => e.stopPropagation()}
          >
            {/* Close Button */}
            <button
              onClick={() => setZoomImage(null)}
              className="absolute top-4 right-4 w-8 h-8 rounded-full bg-white border border-[#E6DFD5] flex items-center justify-center text-[#4F4A45] hover:bg-[#1E1B18] hover:text-white hover:border-[#1E1B18] transition-all duration-200 cursor-pointer z-10 font-bold"
            >
              ✕
            </button>

            {/* Achievement Image Frame */}
            <div className="w-full flex-1 min-h-0 flex items-center justify-center bg-white border border-[#E6DFD5] rounded-xl p-3 shadow-inner mt-4 overflow-hidden">
              <img
                src={zoomImage.src}
                alt={zoomImage.title}
                className="max-w-full max-h-full object-contain rounded-lg border border-[#FAF6F0]"
              />
            </div>

            {/* Caption */}
            <div className="text-center w-full px-2">
              <h3 className="handwritten-title text-2xl text-[#1E1B18] leading-tight">
                {zoomImage.title}
              </h3>
              <p className="handwritten-text text-sm text-[#4F4A45] mt-2 max-w-md mx-auto">
                {zoomImage.desc}
              </p>
              <span className="font-mono text-xs text-[#9B938B] block mt-2">
                -- {zoomImage.date}
              </span>
            </div>
          </div>
        </div>,
        document.body
      )}
    </div>
  );
}